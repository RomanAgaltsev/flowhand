// Package workflows will be the Postgres-backed repository for the workflows
// bounded context (Phase 2). It is a placeholder so the import path and layer
// rules are fixed from day one.
//
// Layer: persistence. Same import rules as internal/repository/tasks.
package workflows
