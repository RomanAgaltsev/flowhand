// Package schedules will be the Postgres-backed repository for the schedules
// bounded context (Phase 1 cron). It is a placeholder so the import path and
// layer rules are fixed from day one.
//
// Layer: persistence. Same import rules as internal/repository/tasks.
package schedules
