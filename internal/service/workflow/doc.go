// Package workflow will hold the DAG use cases (Phase 2). It is a placeholder
// so the import path and layer rules are fixed from day one.
//
// Layer: service. Same import rules as internal/service/task.
package workflow
